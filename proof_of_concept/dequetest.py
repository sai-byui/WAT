from collections import deque

merp, burp, snurp = raw_input("3 words:").split(" ");

lisst = []
lisst.append(merp, burp, snurp)
print(lisst)
deck = deque

deque.append(merp)

#Conclusions drawn from this: It seems 'Deques' are only used for character
#elements
#Just use lists!
